<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:animateLayoutChanges="true"
    android:theme="@style/Theme.AppCompat.Light"
    >

    <data>


    </data>

    <RelativeLayout
        android:id="@+id/layout_image_container"
        android:layout_width="100dp"
        android:layout_height="100dp"
        >

        <ImageView
            android:id="@+id/imagen"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginStart="5dp"
            android:layout_marginTop="5dp"
            android:layout_marginEnd="5dp"
            android:layout_marginBottom="5dp"
            android:scaleType="fitCenter"
            android:transitionName="prev_imagegallery"
            tools:src="@tools:sample/avatars"
            />

        <!-- material check box -->
        <!-- android:background="@{isSelected ? @drawable/ic_check_filled : @drawable/ic_check_unfilled}" -->
        <ImageButton
            android:id="@+id/marcar_para_borrar"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:layout_marginEnd="5dp"
            android:layout_marginBottom="5dp"
            android:visibility="gone"
            />

        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/boton_anadir_imagen"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/white"
            android:visibility="gone"
            android:src="@drawable/ic_baseline_camera_alt_24" />

    </RelativeLayout>

</layout>